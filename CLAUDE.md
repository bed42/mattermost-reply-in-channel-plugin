# CLAUDE.md

## MANDATORY: Use td for Task Management (if its present)

Run td usage --new-session at conversation start (or after /clear). This tells you what to work on next.

Sessions are automatic (based on terminal/agent context). Optional:

- td session "name" to label the current session
- td session --new to force a new session in the same context

Use td usage -q after first read.

## Mandatory: if the human does not provide an existing task for the work you've been asked to start, create a task or tasks with td first and use td to track progress (if its present)

---

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.
Be accurate with responses and avoid hyperbole.

## Project Overview

Read README.md for this
